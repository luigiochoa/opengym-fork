# Runtime local — branding is injected by apply-brand.sh
RP_ID=localhost
ORIGIN=http://localhost:8080
WEB_PORT=8080

INVITE_ONLY=1
ALLOW_GUEST=0

# Set after creating the owner profile:
# ADMIN_UIDS=user-id

