# Exercise media — fetched on first `docker compose up` (or via scripts/fetch-media.sh)
media/img/
media/gif/

# Runtime data — users, passkeys, per-user state, session secret
data/
backups/

# Runtime instance config (never commit domain/admin/secrets)
/.env

# Build output / deps
frontend/dist/
node_modules/
*.log
.DS_Store
node_modules
frontend/public/brand-logo.png
frontend/public/brand-logo.svg
